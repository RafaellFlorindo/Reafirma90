# Copie este arquivo para .env.local e preencha com os valores reais.

# URL do webhook que vai receber os dados capturados no quiz
# (nome, e-mail, whatsapp + respostas) via POST, em JSON.
NEXT_PUBLIC_WEBHOOK_URL=

# URL de checkout do produto Reafirma 90 (R$47).
NEXT_PUBLIC_CHECKOUT_URL=
